bind 0.0.0.0
port 6379
replicaof no one
# requirepass is passed via command-line --requirepass "${REDIS_PASSWORD}" to keep
# credentials out of tracked config. Never commit a password here.
appendonly yes
save 900 1
save 300 10
save 60 10000
